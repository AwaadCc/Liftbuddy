import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.awaadc.liftbuddy',
  appName: 'Liftbuddy',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
